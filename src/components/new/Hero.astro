<section class="hero" id="inicio">
    <div class="container">
        <div class="hero-content">
            <div class="hero-text animate-on-scroll">
                <h1>Impulsa tu futuro con nuestros cursos</h1>
                <p>En nuestra empresa, ofrecemos formación de alta calidad en las áreas más demandadas del mercado laboral. Nuestros cursos están diseñados para proporcionarte las habilidades y conocimientos necesarios para destacar en tu carrera profesional.</p>
                <div class="hero-buttons">
                    <a href="#cursos" class="btn">Explorar Cursos</a>
                    <a href="#sobre-nosotros" class="btn btn-outline">Conocer Más</a>
                </div>
            </div>
            <div class="hero-image animate-on-scroll">
                <img src="https://hebbkx1anhila5yf.public.blob.vercel-storage.com/chicos_estudiando-gXsHY6RLD5DYNoBgjtB6BvVCznjooF.png" alt="Estudiantes aprendiendo">
            </div>
        </div>
    </div>
</section>

<style>
    
      .hero {
            background: linear-gradient(135deg, #f7fafc 0%, #edf2f7 100%);
            padding: 100px 0;
            position: relative;
            overflow: hidden;
        }

        .hero::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><defs><pattern id="grain" width="100" height="100" patternUnits="userSpaceOnUse"><circle cx="25" cy="25" r="1" fill="%23667eea" opacity="0.05"/><circle cx="75" cy="75" r="1" fill="%23764ba2" opacity="0.05"/></pattern></defs><rect width="100" height="100" fill="url(%23grain)"/></svg>');
            pointer-events: none;
        }

        .hero-content {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 60px;
            align-items: center;
            position: relative;
            z-index: 1;
        }

        .hero-image {
            border-radius: 24px;
            overflow: hidden;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.1);
            transform: perspective(1000px) rotateY(-5deg);
            transition: transform 0.3s ease;
        }

        .hero-image:hover {
            transform: perspective(1000px) rotateY(0deg);
        }

        .hero-image img {
            width: 100%;
            height: 400px;
            object-fit: cover;
        }

        .hero-text h1 {
            font-size: clamp(32px, 5vw, 56px);
            font-weight: 700;
            margin-bottom: 24px;
            line-height: 1.1;
            background: linear-gradient(135deg, #1a1a1a 0%, #4a5568 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .hero-text p {
            font-size: 18px;
            color: #4a5568;
            margin-bottom: 32px;
            line-height: 1.7;
        }

        .hero-buttons {
            display: flex;
            gap: 16px;
            flex-wrap: wrap;
        }


    </style>